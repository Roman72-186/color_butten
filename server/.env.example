OPENROUTER_API_KEY=

# Токен для GET /api/analytics/stats (заголовок Authorization: Bearer <токен>) —
# придумать длинную случайную строку, ввести в скрытой вкладке «Аналитика» (открывается
# тройным кликом по версии в подвале мини-аппа). Без переменной эндпоинт статистики
# всегда отвечает 401.
ANALYTICS_ADMIN_TOKEN=
