# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Секреты локальной разработки (vercel dev)
.env
.env.local

# Данные SQLite бэкенда аналитики (server/db.js) — не коммитим
server/data/

# Сохранённые сессии — только в закрытом репозитории workspace, в каталоге
# уровнем выше (ai-clone/projects/цветне кнопки/session-handoffs/). Этот репозиторий
# публичный, а в handoff попадают внутренние заметки: состояние сервера, доступы,
# незакрытые риски. Здесь папку не заводить.
session-handoffs/
